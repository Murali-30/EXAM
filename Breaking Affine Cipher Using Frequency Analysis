6. Breaking Affine Cipher Using Frequency Analysis
#include <stdio.h>

int gcd(int a, int b) {
    if (b == 0) return a;
    return gcd(b, a % b);
}

int main() {
    printf("Breaking Affine Cipher: Frequency Analysis required.\n");
    return 0;
}
